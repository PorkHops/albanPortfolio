@using MudBlazor
@using MudBlazor.Services
@using MudBlazor.Components
@using albanPortfolio.Components.Utils

@inject IHttpContextAccessor _context

@inherits LayoutComponentBase

<HeadContent>
    <script src="js/paint.js"></script>
</HeadContent>

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />
<MudPopoverProvider />

    <MudLayout>
        <MudAppBar>
            <MudContainer MaxWidth="MaxWidth.Large" Class="d-flex justify-content-center">
                <MudText Typo="Typo.h6">Alban's Portfolio</MudText>
            </MudContainer>
            <MudSpacer />
            <Contact Size="Size.Small" Justify="Justify.FlexStart" />
        </MudAppBar>
        <MudDrawer Variant="DrawerVariant.Persistent"
                   Docked="true"
                   Open="true"
                   ClipMode="@DrawerClipMode.Always"
                   Width="200px">
            <NavMenu />
        </MudDrawer>
        <MudMainContent Class="pt-16">
            @Body
        </MudMainContent>
    </MudLayout>

@code{
    // private string userIP { get; set; }

    protected override void OnInitialized()
    {
        // userIP = _context.HttpContext?.Connection?.RemoteIpAddress?.ToString() ?? "";
    }
}